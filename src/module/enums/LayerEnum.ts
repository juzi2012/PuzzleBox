/**层次枚举 */
enum LayerEnum {
    /** 最下面的游戏场景的层 */
    SCENE,
    /** ui模块层 */
    UI,
    /** 静态层，显示聊天窗等非场景组件 */
    STATIC,
    /** 弹出窗层 */
    POPUP,
    /** 顶层，显示加载,新手教程，debug工具，Tips等ui */
    TOP

}